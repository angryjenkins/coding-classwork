console.log(process.argv);

var addThese = process.argv;
var count = 2;

for(var i=2;i<(process.argv).length;i++){
  if(i != count){
    addThese.push(parseFlost(process.argv[i]));

    for(var i = 0; i < addThese.length; i++){
      var sum;
      sum += addThese[i];
    }

      // Iterate over your first array and then grab the second element add the values up
  }
    console.log(v);
    count++;

  }
